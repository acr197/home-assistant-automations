# secrets and tokens
secrets.yaml
*.db
*.db-shm
*.db-wal
home-assistant_v2.db*

# logs and runtime
home-assistant.log*
OZW_Log.txt
*.log
*.pid

# caches
.storage/
__pycache__/
*.pyc
.cache/

.ha_run.lock

# project instructions (kept on the Pi, not published)
CLAUDE.md

# runtime data and credential caches (regenerated by HA; never publish)
deps/
tts/
nest/
.cloud/

# editor / UI backup copies
*.bak
